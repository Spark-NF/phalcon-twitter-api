To launch the app, you must configure the nginx to set the root in `public/`.

You also need a mysql running with this config :
- username = root
- password = 
- host = localhost
- database = phalcon
